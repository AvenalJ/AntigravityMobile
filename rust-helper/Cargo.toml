[package]
name = "desktop-helper"
version = "0.1.0"
edition = "2021"
description = "Full-desktop capture + input helper for Antigravity Mobile (scrap + enigo)"

[[bin]]
name = "desktop-helper"
path = "src/main.rs"

[dependencies]
# Phase 1: launcher/supervisor + localhost WebSocket
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time", "net", "process", "signal"] }
tokio-tungstenite = "0.21"
futures-util = { version = "0.3", default-features = false, features = ["sink", "std"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
env_logger = "0.11"
# Phase 2: desktop capture + JPEG encode
scrap = "0.5"
image = { version = "0.25", default-features = false, features = ["jpeg"] }
# Phase 4: native input injection (mouse + keyboard with modifiers)
enigo = "0.2"
# H.264 screen streaming: software encoder (vendored OpenH264 build).
openh264 = "0.6"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
